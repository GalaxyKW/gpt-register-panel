{
  "name": "gpt-register-panel",
  "version": "0.1.0",
  "private": false,
  "description": "Read-only account and token diff panel for gpt_register and Sub2API.",
  "main": "backend/index.js",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "snapshot": "node backend/cli/snapshot.js",
    "snapshot:sub2api": "node backend/cli/snapshot.js --with-sub2api",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=18.17"
  },
  "dependencies": {
    "sql.js": "^1.14.2"
  }
}
